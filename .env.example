# Backend Configuration
# Database
DB_URL=jdbc:postgresql://postgres-db:5432/community_db
DB_USER=user
DB_PASSWORD=password

# Firebase
# Path to the service account JSON file
FIREBASE_CONFIG_PATH=/app/firebase-service-account.json

# Frontend Configuration
# Note: Frontend environments are typically built at build time. 
# See frontend/src/environments/environment.example.ts for structure.
